# 兼容入口

本目录保留了旧的 `scripts/*` 文件名，但它们现在只是**兼容 wrapper**：
真正的实现位于 `../skills/paper-review-kit/scripts/`（单一事实源）。

新项目请直接复制 `skills/paper-review-kit/scripts/` 到项目里，不要再复制本目录。

说明见 `skills/paper-review-kit/scripts/README.md`。
